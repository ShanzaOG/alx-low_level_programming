#include "main.h"
/**
 * add - adds two integers
 * @a: takes integer arguments a
 * @b: takes another integer b
 * Description: adds a and b
 * Return: integer result
 */
int add(int a, int b)
{
	int c = 0;

	c = a + b;
	return (c);
}
