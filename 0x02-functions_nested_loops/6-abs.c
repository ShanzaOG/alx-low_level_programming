#include "main.h"
/**
 * _abs - computes absolute value
 * @r:integer value argument
 * Description: Function returns the absolute value
 * Return: integer
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}

