#include "main.h"
/**
 * Function to print alphabets
 *
 * Return: doesnt return anything
 */
void print_alphabet(void)
{
	char str;

        for (str = 'a'; str <= 'z'; str++)
                _putchar(str);
        _putchar('\n');
}
